apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: database-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce # see readme for different types of access modes
  resources:
    requests:
      storage: 2Gi # 2 giga bytes